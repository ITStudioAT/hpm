These feature tests exercise each public method of the `AdminController`.
Each test file is named after its corresponding controller method, and mocks are
bound into the service container to keep the tests isolated and fast.
