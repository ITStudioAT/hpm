These feature tests exercise each public method of the `AdminController`.
Each test file is named after its corresponding controller method and runs in a
separate process to isolate Mockery's class overloading.
